{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && yarn build",
    "install-client": "cd .. && cd client && yarn install",
    "heroku-postbuild": "yarn install-client && yarn build",
    "old-start": "cd .. && cd client && yarn start",
    "start": "nodemon server.js",
    "dev": "concurrently -n 'server,client' -c 'red,green'  \"nodemon server.js\" \"yarn old-start\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "concurrently": "^5.3.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.7"
  }
}
